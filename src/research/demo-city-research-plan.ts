import { readDemoRelocationSeed, type DemoLocalizedName } from "./demo-relocation-seed";
export type DemoCityResearchJob=Readonly<{schemaVersion:"demo-city-research-job@1";jobId:string;countryCode:string;cityId:string;countryName:DemoLocalizedName;cityName:DemoLocalizedName;localeHints:readonly string[];selectionRole:"national_capital"|"relocation_city";authority:"none_demo_seed_only"}>;
export type DemoCityResearchBatch=Readonly<{schemaVersion:"demo-city-research-batch@1";batchId:string;jobs:readonly [DemoCityResearchJob,DemoCityResearchJob,DemoCityResearchJob,DemoCityResearchJob,DemoCityResearchJob]}>;
const jobs=Object.freeze(readDemoRelocationSeed().countries.flatMap(country=>country.cities.map(city=>Object.freeze({schemaVersion:"demo-city-research-job@1" as const,jobId:`demo-city-research:${country.countryCode.toLowerCase()}:${city.cityId}`,countryCode:country.countryCode,cityId:city.cityId,countryName:Object.freeze({...country.name}),cityName:Object.freeze({...city.name}),localeHints:Object.freeze([...country.localeHints]),selectionRole:city.selectionRole,authority:"none_demo_seed_only" as const}))));
const batches=Object.freeze(Array.from({length:10},(_,index)=>Object.freeze({schemaVersion:"demo-city-research-batch@1" as const,batchId:`demo-city-research-batch:${String(index+1).padStart(2,"0")}`,jobs:Object.freeze(jobs.slice(index*5,index*5+5)) as DemoCityResearchBatch["jobs"]})));
export function planDemoCityResearchJobs():readonly DemoCityResearchJob[]{return jobs;} export function planDemoCityResearchBatches():readonly DemoCityResearchBatch[]{return batches;}
