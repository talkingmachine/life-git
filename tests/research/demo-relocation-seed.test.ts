import { readFileSync } from "node:fs";
import { expect,test } from "vitest"; import { readDemoRelocationSeed } from "../../src/research/demo-relocation-seed";
const expected=[
["SI","ljubljana,maribor,koper,celje,kranj","sl,en"],["PT","lisbon,porto,braga,coimbra,funchal","pt,en"],["ES","madrid,barcelona,valencia,alicante,malaga","es,en"],["DE","berlin,munich,hamburg,frankfurt-am-main,dusseldorf","de,en"],["RS","belgrade,novi-sad,nis,subotica,kragujevac","sr,en"],["ME","podgorica,budva,bar,herceg-novi,tivat","cnr,sr,en"],["GE","tbilisi,batumi,kutaisi,rustavi,poti","ka,en"],["TR","ankara,istanbul,izmir,antalya,mersin","tr,en"],["AE","abu-dhabi,dubai,sharjah,ajman,ras-al-khaimah","ar,en"],["TH","bangkok,chiang-mai,phuket-city,pattaya,hua-hin","th,en"]] as const;
test("owns the exact frozen subjective ten by five seed",()=>{const seed=readDemoRelocationSeed();expect(Object.keys(seed)).toEqual(["schemaVersion","purpose","countries"]);expect(seed.schemaVersion).toBe("demo-relocation-seed@1");expect(seed.countries.map(x=>[x.countryCode,x.cities.map(c=>c.cityId).join(","),x.localeHints.join(",")])).toEqual(expected);expect(seed.countries.every(x=>Object.keys(x).join(",")==="countryCode,name,localeHints,cities"&&x.cities[0]!.selectionRole==="national_capital"&&x.cities.filter(c=>c.selectionRole==="national_capital").length===1)).toBe(true);expect(new Set(seed.countries.flatMap(x=>x.cities.map(c=>`${x.countryCode}:${c.cityId}`))).size).toBe(50);expect(Object.isFrozen(seed.countries[0]!.cities[0]!.name)).toBe(true);expect(JSON.stringify(seed)).not.toMatch(/url|source|fact|color|verdict|package|manifest|evidence|knowledge|frontier|revision|status/i);expect(readFileSync(new URL("../../src/research/demo-relocation-seed.ts",import.meta.url),"utf8")).not.toMatch(/^import /m);});
